
<% if member_signed_in? %>

  
  <h3>you have succesfully signed in</h3>

<h1>Listing Posts</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Title</th>
      <th>Url</th>
      <th>Createdat</th>
      <th>Content</th>
      <th>Member</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr >
        <td><%= post.name %></td>
        <td><%= post.title %></td>
        <td><%= post.url %></td>
        <td><%= post.createdat %></td>
        <td><%= post.content %></td>
        <td><%= post.member %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>



  <%= link_to "Sign Out", destroy_member_session_path, :method => :delete %>
  

<% else %>


  <table>
  
<tr>
  <td><%= link_to "Sign in", new_member_session_path %></td>
  <td>  </td>
  <td>  </td>
  <td>  </td>
  <td><%= link_to "Sign up", new_member_registration_path%></td>
  </tr>
  </table>
<% end %>

